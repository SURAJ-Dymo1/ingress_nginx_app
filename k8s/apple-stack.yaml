# 1. THE DEPLOYMENT (The App)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: apple-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: apple
  template:
    metadata:
      labels:
        app: apple
    spec:
      containers:
      - name: apple-container
        image: surajmane121045/ingress_app_one:b26f8873e03f654d97cf56d66b0b9e936d70d56b
        ports:
        - containerPort: 80

---
# 2. THE SERVICE (The Internal Network Name)
apiVersion: v1
kind: Service
metadata:
  name: apple-service
spec:
  selector:
    app: apple # This MUST match the label in the Deployment
  ports:
    - protocol: TCP
      port: 80         # Port the Service listens on
      targetPort: 80 # Port the Pod is listening on