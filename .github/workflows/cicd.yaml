name: Three Tier CI/CD Pipeline

on:
  push:
    branches:
      - main
permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set Image Tag
      run: echo "IMAGE_TAG=${{ github.sha }}" >> $GITHUB_ENV

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # ---------------- FRONTEND App1 ----------------
    - name: Build Frontend Image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/ingress_app_one:${IMAGE_TAG} ./App_One
        docker push ${{ secrets.DOCKER_USERNAME }}/ingress_app_one:${IMAGE_TAG}

    # ---------------- Frontend App2 ----------------
    - name: Build Backend Image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/ingress_app_two:${IMAGE_TAG} ./App_Two
        docker push ${{ secrets.DOCKER_USERNAME }}/ingress_app_two:${IMAGE_TAG}

    # ---------------- DATABASE ----------------
    # - name: Build Database Image
    #   run: |
    #     docker build -t ${{ secrets.DOCKER_USERNAME }}/three_tier_app_mongodb:${IMAGE_TAG} ./database
    #     docker push ${{ secrets.DOCKER_USERNAME }}/three_tier_app_mongodb:${IMAGE_TAG}

    # ---------------- UPDATE K8S MANIFESTS ----------------
    - name: Update Kubernetes Manifests
      run: |
        sed -i "s|image: .*ingress_app_one:.*|image: ${{ secrets.DOCKER_USERNAME }}/ingress_app_one:${IMAGE_TAG}|g" k8s/apple-stack.yaml
        sed -i "s|image: .*ingress_app_two:.*|image: ${{ secrets.DOCKER_USERNAME }}/ingress_app_two:${IMAGE_TAG}|g" k8s/banana-stack.yaml
# sed -i "s|image: .*three_tier_app_mongodb:.*|image: ${{ secrets.DOCKER_USERNAME }}/three_tier_app_mongodb:${IMAGE_TAG}|g" k8s-manifests/mongo-deployment.yaml

    - name: Commit Updated Manifests
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "actions@github.com"
        git add k8s-manifests/
        git commit -m "Update image tags to ${IMAGE_TAG} [skip ci]" || echo "No changes to commit"
        git push